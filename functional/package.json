{
  "name": "lesson2-intro-html",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode dev",
    "build": "vite build",
    "preview": "vite preview",
    "vitestui": "vitest --ui",
    "server:simple": "simple-server dist 3000",
    "server:json": "json-server --watch db.json --port 3003 --static dist --delay 2000",
    "cy:open": "yarn dev & cypress open --e2e --config-file=cypress.config.js",
    "cy:run": "yarn dev & cypress run --e2e --config-file=cypress.config.js"
  },
  "devDependencies": {
    "@vitest/ui": "^0.25.1",
    "cypress": "^10.10.0",
    "json-server": "^0.17.1",
    "prettier": "^2.7.1",
    "simple-server": "^1.1.1",
    "vite": "^3.1.0",
    "vite-plugin-mkcert": "^1.10.1",
    "vitest": "^0.25.1"
  },
  "prettier": {
    "printWidth": 120,
    "singleQuote": true
  }
}
